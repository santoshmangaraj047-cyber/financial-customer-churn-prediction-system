A full-stack role-based web application that predicts whether a bank customer is likely to churn using machine learning techniques.

This system provides separate dashboards for Bank Users and Administrators, implementing secure authentication and role-based access control.

Bank dashboard overview now includes inline dataset upload and prediction analytics tools for bank users.

## Latest Update Highlights
- Bank dashboard now includes inline dataset upload + persistent analytics sessions.
- Batch pipeline supports CSV, XLSX, and XLS inputs for both bank/admin flows.
- Analytics now includes region-wise/account-type churn insights and high-risk export options.

